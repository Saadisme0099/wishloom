'use client'
import Link from 'next/link';import {useState} from 'react';import {Heart,LayoutDashboard,Library,Plus,LogOut,Menu,X} from 'lucide-react';import {supabase} from '@/lib/supabase'
export default function Nav(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-30 border-b border-black/5 bg-[#fffaf5]/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><Link href="/" className="flex items-center gap-2 font-black tracking-tight" onClick={()=>setOpen(false)}><span className="grid h-9 w-9 place-items-center rounded-2xl bg-[#1b1820] text-white"><Heart size={17} fill="currentColor"/></span><span>Wishloom</span></Link><nav className="hidden items-center gap-2 sm:flex"><Link className="btn btn-soft" href="/dashboard"><LayoutDashboard size={16}/>Dashboard</Link><Link className="btn btn-soft" href="/library"><Library size={16}/>Library</Link><Link className="btn btn-primary" href="/create"><Plus size={17}/>Create</Link></nav><div className="flex items-center gap-1"><button className="rounded-full p-2 hover:bg-black/5 sm:hidden" onClick={()=>setOpen(v=>!v)} aria-label="Open menu">{open?<X size={20}/>:<Menu size={20}/>}</button><button className="rounded-full p-2 hover:bg-black/5" onClick={async()=>{await supabase().auth.signOut();location.href='/'}} aria-label="Log out"><LogOut size={18}/></button></div></div>{open&&<nav className="border-t border-black/5 px-5 py-3 sm:hidden"><div className="mx-auto grid max-w-6xl gap-2"><Link className="btn btn-soft justify-start" href="/dashboard" onClick={()=>setOpen(false)}><LayoutDashboard size={16}/>Dashboard</Link><Link className="btn btn-soft justify-start" href="/library" onClick={()=>setOpen(false)}><Library size={16}/>Library</Link><Link className="btn btn-primary justify-start" href="/create" onClick={()=>setOpen(false)}><Plus size={17}/>Create</Link></div></nav>}</header>}
