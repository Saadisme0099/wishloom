'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {Heart} from 'lucide-react';import {supabase} from '@/lib/supabase'
export default function Login(){const [mode,setMode]=useState<'login'|'signup'>('login');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [msg,setMsg]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setMsg('');const s=supabase();if(mode==='login'){const {error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else router.push('/dashboard')}else{const {error}=await s.auth.signUp({email,password,options:{data:{display_name:name}}});if(error)setMsg(error.message);else setMsg('Check your email to confirm your account, then log in.')}}return <main className="min-h-screen bg-[#fffaf5] px-5 py-8"><div className="mx-auto max-w-md"><Link href="/" className="flex items-center gap-2 font-black"><span className="grid h-9 w-9 place-items-center rounded-2xl bg-[#1b1820] text-white"><Heart size={17} fill="currentColor"/></span>Wishloom</Link><div className="card shadow-soft mt-16 p-8"><p className="text-sm font-bold uppercase tracking-[.2em] text-[#a65b67]">{mode==='login'?'Welcome back':'Start your loom'}</p><h1 className="serif mt-3 text-4xl">{mode==='login'?'Open your studio.':'Make something lovely.'}</h1><form onSubmit={submit} className="mt-8 space-y-4">{mode==='signup'&&<input className="input" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)} required/>}<input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password (8+ characters)" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn btn-primary w-full">{mode==='login'?'Log in':'Create account'}</button>{msg&&<p className="rounded-2xl bg-[#fff1f2] p-3 text-sm text-[#8a4753]">{msg}</p>}</form><button className="mt-5 w-full text-sm font-semibold text-[#7a6d75]" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?"Don't have an account? Create one":"Already have an account? Log in"}</button></div></div></main>}
